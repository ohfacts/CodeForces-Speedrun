// my 2nd program on codeforces. Lost the original file. Copied the same code to preserve authenticity
// this and the previous one were written in visual studio originally. Not very good for competetive programming 
#include <iostream>
using namespace std;

int main()
{	
	int a;
	cin >> a;
	if (a % 2 == 0 && a >= 4)
		cout << "YES";
	else
		cout << "NO";

	return 0;
}